server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/revjobs_applications?createDatabaseIfNotExist=true
    username: root
    password: Vishnu@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: application-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true